## concat

### What to build
```go
func Concat(str1 string, str2 string) string {

}
```
Return the concatenation of `str1` and `str2`.

**Usage:**
```go
piscine.Concat("Hello!", " How are you?")
```
```console
$ go run .
Hello! How are you?
$
```

### Stuck? Ask better
Show your return statement — if the output has an unexpected extra space or is missing one, check whether that space is already inside one of the input strings rather than something you need to add.
